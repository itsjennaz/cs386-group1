const assert = require('assert')
const fileToTest = require('../fileToTest.js')

describe('nameOfTest', function() {
    it('function call should return x', function() {
        assert.equal(funcCall(), expectedReturn)
    }
}
